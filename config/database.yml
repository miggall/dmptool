defaults: &defaults
  adapter: <%= Rails.configuration.x.system.database_adapter %>
  encoding: <%= Rails.configuration.x.system.database_adapter == "mysql2" ? "utf8mb4" : "" %>
  database: <%= Rails.configuration.x.system.database_name %>
  pool: <%= Rails.configuration.x.system.database_pool_size %>
  host: <%= Rails.configuration.x.system.database_host %>
  username: <%= Rails.configuration.x.system.database_username %>
  password: <%= Rails.configuration.x.system.database_password %>

development:
  <<: *defaults

test:
  <<: *defaults

  database: "dmp_test"

stage:
  <<: *defaults

production:
  <<: *defaults
